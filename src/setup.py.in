# -*- coding: UTF-8 -*-
# setup.py: Install-Script

try:
  from setuptools import setup
except:
  from distutils import setup
  
from distutils.sysconfig import get_python_lib

setup(name = 'pyavrs',
      version = "@PACKAGE_VERSION@",
      py_modules = ["pyavrs"],
      data_files = [(get_python_lib(), ["_pyavrs@PYTHON_MODULE_EXTENSION@"])],
      author="Klaus Rudolph & others",
      author_email="<avrs-develop@lists.sourceforge.net>",
      license="GPL",
      description="python modul for avrs")
# EOF
